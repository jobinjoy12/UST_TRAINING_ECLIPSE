import java.util.*;

class Solution {
    public int longestConsecutive(int[] nums) {
        // Hint: Use a HashSet to achieve O(n) lookup
        Set<Integer> numSet = new HashSet<>();
        
        // 1. Add all numbers to the set
        
        // 2. Iterate and check for sequence starts
        
        // Your logic here
        if(nums == null)
        {
            return 0;
        }
        for(int n : nums)
        {
            numSet.add(n);

        }
        boolean flag = false;

        
        
        for(int i =0;i<numSet.size();i++)
        {
            numSet.
            if(numSe[)
        }
        return 0; // Placeholder
    }
}