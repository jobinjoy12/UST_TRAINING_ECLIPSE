sonar.projectKey=my_java_app_key
    sonar.projectName=My Java Application
    sonar.projectVersion=1.0
    sonar.sources=.
    sonar.java.binaries=.
    # If your SonarQube server is not on localhost:9000, update this URL
    sonar.host.url=http://localhost:9000
    # If your SonarQube server requires a token, uncomment and add it
    # sonar.login=YOUR_SONARQUBE_TOKEN